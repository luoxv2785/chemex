# 作者：Celaraze
# 时间：2020年10月22日
# 如果你的项目根目录没有 .env 文件，请复制我一份，然后重命名为 .env
# 这是 Chemex 运行的关键配置，也是每次部署之后必须做的第一件事

APP_NAME=Laravel
APP_ENV=local
APP_URL=http://127.0.0.1:8000

# 应用密钥，可以通过执行 php artisan key:generate 重新生成
APP_KEY=base64:aRpYlARLPcqF3VlZ6jrT1vUX4aWzG0xFYriKzLO3zqE=

# 是否开启 DEBUG 模式
# 开启后如果程序出错，将会抛出异常
# 关闭后如果程序出错，将只会给出 HTTP 错误码 500
APP_DEBUG=true

LOG_CHANNEL=stack

# 数据库连接驱动（不用改动，无论你使用的是 MySQL 还是 MariaDB ）
DB_CONNECTION=mysql
# 数据库地址
DB_HOST=127.0.0.1
# 数据库端口号
DB_PORT=3306
# 数据库名称
DB_DATABASE=chemex_alone
#DB_DATABASE=/Users/celaraze/Desktop/chemex/database.db
# 数据库用户名
DB_USERNAME=root
# 数据库密码
DB_PASSWORD=root

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=null
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# 如果站点使用HTTPS，请改为true启用它
ADMIN_HTTPS=false
# 决定了此实例是否以demo运行（部分权限受阻）
ADMIN_DEMO=false
# JWT密钥，可以通过执行 php artisan jwt:secret 重新生成
JWT_SECRET=unx5sw0jJOlbvH2KVz4QPsbsGmdwqwcZshqsyyfU4YjviRtTvjSzckxcKyogvdFb

OCTANE_SERVER=swoole
OCTANE_HTTPS=false
